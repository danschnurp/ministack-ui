version: "3.8"

services:
  ministack:
    image: ministackorg/ministack
    ports:
      - "4566:4566"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - REDIS_HOST=redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
